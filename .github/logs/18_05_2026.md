# 18_05_2026

- verified the current artifact split directly against `quinn-amc-vps`: canonical processed evidence, figure outputs, and packaged report are current locally from the `2026-05-17` VPS rerun, but the local workspace is not a full raw-result mirror
- rewrote `README.md` into a shorter operator guide that states the AMC v1 completion boundary and the actual local-versus-VPS artifact status
- rewrote `docs/methodology.md` into the single canonical scope, workflow, evidence, and reproduction spec so the repository stops repeating the same phase-freeze story across multiple notes
- removed redundant phase and status notes that had drifted into overlapping copies of the same completion, evidence, and VPS handoff narrative
- removed stale repo-local sprint agent and prompt documents that referenced non-existent `sprints/` materials
- synced `TODO.md` and `.github/copilot-instructions.md` with the consolidated documentation surface
- split artifact storage into `results/local` for local suites and `results/vps` for canonical VPS outputs and scp-backed copies
- updated suite configs, compose defaults, harness/report defaults, and the VPS runner so path routing follows the new local-versus-VPS split
